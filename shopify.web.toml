name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

# The Supabase schema is managed via Supabase migrations (prisma db pull to
# sync) — prisma migrate deploy must not run against it.
[commands]
predev = "npx prisma generate"
dev = "npm exec react-router dev"
